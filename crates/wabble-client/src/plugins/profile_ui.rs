use bevy::prelude::*;

/// Placeholder for player profile/stats UI. Will be implemented in later phases.
pub struct ProfilePlugin;

impl Plugin for ProfilePlugin {
    fn build(&self, _app: &mut App) {}
}
