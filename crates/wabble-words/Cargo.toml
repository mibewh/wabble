[package]
name = "wabble-words"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
wabble-platform = { path = "../wabble-platform" }
serde = { version = "1", features = ["derive"] }
rand = "0.8"

[dev-dependencies]
rand = "0.8"
